version: 1
applications:
  - appRoot: apps/web
    frontend:
      framework: none
      phases:
        preBuild:
          commands:
            - yarn install
        build:
          commands:
            - yarn turbo run build --filter=web
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - .yarn/cache/**/*
          - node_modules/**/*
